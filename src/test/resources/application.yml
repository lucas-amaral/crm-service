spring:

  jpa:
    hibernate:
      ddl-auto: validate

  datasource:
    url: jdbc:postgresql://postgres:5432/crm
    username: postgres
    password: internal123
    driverClassName: org.postgresql.Driver